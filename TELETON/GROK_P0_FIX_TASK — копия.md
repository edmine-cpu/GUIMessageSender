Сделай только P0-правки. Не переписывай архитектуру целиком и не трогай параллельные кампании пока.

Задачи:

1. Stop для циклической рассылки
- Найди общий обработчик Stop / _stop_current_process.
- Сделай так, чтобы он также останавливал циклическую рассылку через _cycle_stop_event или общий stop_all.
- Добавь понятный лог: "запрошена остановка циклической рассылки".
- Не ломай отдельную кнопку Stop на вкладке "Циклическая".

2. Фильтр tdata_*
- Найди _scan_sessions_dir и _import_selected_sessions.
- Сделай так, чтобы session-файлы вида session_tdata_*.session или аккаунты phone.startswith("tdata_") не попадали в список импорта и не добавлялись в БД.
- Добавь короткий лог/комментарий, почему они пропускаются.

3. Понятная ошибка TData
- Найди места, где показывается "ИМПОРТ ПРЕРВАН до чтения TData".
- Не делай большой рефакторинг.
- Минимально добавь reason/detail в messagebox и лог: путь, почему не TData, нет key_datas, нет hex-папки, выбрана папка-контейнер или нет прав на sessions.
- Если причина уже есть в res["text"], покажи ее пользователю, а не общий текст.

4. Кнопка "Создать кампанию"
- В CTkInputDialog добавь master=self.
- При пустом имени покажи warning, а не тихий return.
- При ошибке БД покажи showerror.

Ограничения:
- Не трогай отправку сообщений и Telegram-лимиты.
- Не делай worker per campaign_id пока.
- Не меняй БД-схему без необходимости.
- После правок запусти тесты:
  py -3.12 -m pytest tests/ -q

Формат ответа:
1. Какие файлы изменил.
2. Что конкретно изменил.
3. Какие тесты прошли.
4. Какие риски остались.
